declare module h{}
declare module f{}
let t1;let t2;
void function fh() {
    return h / Math.asin(w + t1) + f / Math.acos(w - t2)
}
void function fh(){
    return f / Math.asin(w - t1) + h / Math.acos(w + t2)
}
void function RETURN() {
        void(`RET`+`URN`)
        h / Math.acos(w + t1) + f / Math.acos(w - t2)
        f / Math.asin(w - t2) + h / Math.acos(w + t1)
        void(`RER`+`TUN`)
        h / Math.asin(w - t1) + f / Math.acos(w + t2)
        f / Math.asin(w + t2) + h / Math.asin(w - t1)
        void(`RUR`+`ETN`)
        h / Math.asin(w + t1) + f / Math.asin(w - t2)
        f / Math.acos(w + t2) + h / Math.acos(w - t1)
        void(`RUN`+`ETR`)
         f / Math.asin(w - t1) + h / Math.asin(w + t2)
         h / Math.acos(w - t2) + f / Math.acos(w + t1)
}